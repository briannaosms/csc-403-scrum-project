namespace TowerDefense_TheRPG.code {
  /// <summary>
  /// We ended up not using this. However
  /// this could be used to create different class types
  /// such as Warrior, Mage, Knight, etc. Feel free to use
  /// this for that purpose or discard it if not needed
  /// </summary>
  public abstract class ClassType {
    //public int XP { get; protected set; }
    //public int Level { get; protected set; }
    //public WeaponType WeaponType { get; protected set; }

  }
}
